Java Script Notes

```js
function functionName(parameter, placeholder) {
  return parameter + " " + placeholder;
}

console.log(
  functionName(
    "This is where the argument",
    "are put and passed through the function"
  )
);
```
